import React, { Component } from 'react'
import { Text, View, ScrollView } from 'react-native'

export default class StatisticsScreen extends Component {
    render() {
        return (
            <ScrollView>
                <Text> This will be the Statistics Screen </Text>
            </ScrollView>
        )
    }
}
